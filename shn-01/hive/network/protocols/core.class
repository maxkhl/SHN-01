--[[
    SHN-01 Network Core Protocol Class
        This class represents the CORE protocol in the SHN-01 system.
        It serves as the foundational protocol for basic communications.
]]
local command = newClass(class("../protocol", true))

function command:constructor(hive)
    assert(self, "Must be called on a object")
    self.base:constructor(hive, "CORE", 2015, "Basic communications", {

        -- Define the command sequences here
        -- Example: new("sequence", "commandName", serverFunc, clientFunc)
        --class("../sequence"):create(getAbsolutePath("../sequences/handshake/manifest.lua"), true)
        class("../sequence"):create(hive, self, getAbsolutePath("../sequences/bootstrap/manifest.lua"), true)
    })

end

return command