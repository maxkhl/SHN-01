local bounce = newClass(class("../../sequence"))

function bounce:constructor(content)
    assert(self, "Must be called on a object")
    self.base:constructor("bounce",
        function(message)            
            return client
        end)
end

return bounce
