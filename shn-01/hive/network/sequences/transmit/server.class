local transmit = newClass(class("../../sequence"))

function transmit:constructor(content)
    assert(self, "Must be called on a object")
    self.base:constructor("transmit",
        function(message)
            
            return client
        end)
end

return transmit
